# Base URL of the TaskFlow API, including /api
VITE_API_URL=http://localhost:5000/api

# Show the seeded test accounts on the login screen in a production build.
# They always show in `npm run dev`. Leave this off for a public deployment.
# VITE_SHOW_DEMO_ACCOUNTS=true
